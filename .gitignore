# Python
__pycache__/
*.pyc

# Desktop-app wrapper virtualenv + generated bundle
.venv/
*.app/
_app.iconset/
_iconbase.png

# PyInstaller frozen-build output
build/
dist/

# Runtime writable data — never commit (may contain VIN / vehicle telemetry)
backups/
kline_raw.log
fault_snapshots.log
vanos_events.log
verified_maps.json
drive_log_*.csv
power_log_*.csv
vag_log_*.csv
*.log

# OVPF passport event logs (contain VIN / vehicle data)
passports/
workshop.json
cloud_session.json
vehicle_names.json
hidden_vehicles.json
