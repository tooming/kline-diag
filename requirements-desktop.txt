# Optional desktop-app wrapper (desktop_app.py) — NOT needed for the
# stdlib-only core (power_diag.py, ds2_diag.py, diag_ui.py).
# On macOS pywebview uses the built-in WKWebView via pyobjc (pulled in
# automatically). No Chromium is bundled.
pywebview>=5.0
segno>=1.6   # offline QR generation for the passport
certifi      # CA bundle for ovpf_cloud.py's HTTPS calls -- a frozen build
             # has no OS certificate store to fall back on (see opendiag.spec)
